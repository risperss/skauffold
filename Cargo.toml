[package]
name = "skauffold"
version = "0.1.0"
edition = "2024"

[dependencies]
bitvec = "1"
clap = { version = "4", features = ["derive"] }
rand = { version = "0.8", features = ["std_rng"] }
serde = { version = "1.0", features = ["derive"] }
